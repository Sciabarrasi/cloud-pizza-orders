{
  "name": "process-orders-lambda",
  "version": "1.0.0",
  "description": "Lambda function to process pizza orders from SQS",
  "main": "index.js",
  "scripts": {
    "build": "tsc index.ts",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.450.0",
    "@aws-sdk/lib-dynamodb": "^3.450.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^18.0.0"
  }
}